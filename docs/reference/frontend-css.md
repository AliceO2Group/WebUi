# Frontend - CSS API Reference

The better way to have a CSS reference is to view it in the browser.

```bash
git clone this_repo
cd docs
open showroom.html # from terminal or in your file explorer
```
