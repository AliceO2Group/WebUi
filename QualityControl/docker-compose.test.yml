services:
  test_app:
    depends_on:
      database:
        condition: service_healthy
    build:
      context: .
      target: test
    environment:
      NODE_ENV: test
    tty: true
    container_name: qcg-application-test
