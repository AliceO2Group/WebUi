export class ObjectDomain {
  /**
   * constructor
   * @param {string} id - id
   * @param {string} name - name
   */
  constructor(id, name) {
    this.id = id,
    this.name = name;
  }

  id;

  name;
}
